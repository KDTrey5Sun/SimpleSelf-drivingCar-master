"""v4 package: training and environment for the v4 track.

Make this directory a Python package so that relative imports like
`from .DQN import Agent` resolve to the local v4 modules instead of
shadowed modules at the repository root.
"""
